timeframes:
  base: "1m"

  derived:
    - name: "15m"
      multiple: 15
      closed_bar_rule: "left_closed_right_open"

    - name: "1h"
      multiple: 60

    - name: "6h"
      multiple: 360

    - name: "12h"
      multiple: 720

resample:
  volume_aggregation: "sum"
  price_aggregation:
    open: "first"
    high: "max"
    low: "min"
    close: "last"
